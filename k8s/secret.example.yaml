# 範本 —— 勿放真實金鑰。正式環境改用 SealedSecrets / ExternalSecrets / Vault(CLAUDE.md #8)。
apiVersion: v1
kind: Secret
metadata:
  name: llm-webapp-secrets
  namespace: llm-webapp
type: Opaque
stringData:
  ICA_API_URL: "https://api.example-ica-gateway.example.com/ica"
  ICA_CLAUDE_KEY: "<REPLACE_ME>"
  POSTGRES_PASSWORD: "<REPLACE_ME>"
